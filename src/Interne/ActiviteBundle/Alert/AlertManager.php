<?php

namespace AppBundle\Alert;

/**
 * Class AlertManager
 * Les alertes en oppositions aux actions n'offrent pas de mechanismes de confirmation de la part du membre concerné.
 * Les alertes sont des messages qui sont envoyés au membre, et qui possèdent différentes méthodes utiles.
 * On peut ainsi executer du code au moment où le membre a vu le message et supprimé le message
 * @package AppBundle\Alert
 */
class AlertManager {

    public function send($message) {


    }
}