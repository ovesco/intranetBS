info_groupe:
    label: 'Information sur le groupe scout'
    parametres:
        nom: { type: string, label: 'Nom du groupe scout', default: 'Brigade de Sauvabelin' }
        adresse: { type: text, label: 'Adresse postale', default: 'Adresse du groupe (à modifier)' }
service_mailer:
    label: 'Service de email automatique'
    parametres:
        noReplyEmail: { type: string, label: 'Adresse mail d envoi automatique (ex. no_reply@...)', default: to@modify.ch }
finance:
    label: 'Créances et factures'
    parametres:
        impression_ccp_bvr: { type: string, label: 'Numéro de compte CCP (BVR)', default: 10-1915-8 }
        impression_ccp_bv: { type: string, label: 'Numéro de compte CCP (BV)', default: 10-1915-8 }
        impression_texte_facture: { type: text, label: 'Texte sur les factures', default: 'Text à écrire!!!' }
        impression_affichage_montant: { type: choice, label: 'Affichage du montant sur les factures', options: { choices: [oui, non] }, default: non }
        impression_mode_payement: { type: choice, label: 'Choix du mode de payement', options: { choices: [BV, BVR] }, default: BVR }
intranet:
    label: 'Paramètres généraux du site'
    parametres:
        nom: { type: string, label: 'Nom de l intranet', default: Intranet }
